/**
 * 统一 UI 交互系统 - 基础类型定义
 *
 * 从 @codegraph/union-client 重新导出，保持向后兼容
 */

export {
  InteractionCategory,
  InteractionState,
  type InteractionMetadata,
  type InteractionToolData,
  type InteractionContent,
} from '@codegraph/union-client';
